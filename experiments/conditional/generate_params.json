{
  "_note": "This file is a template. Run evaluate_conditional.py to generate molecules for all condition ranges automatically. Or use this file directly to generate molecules for a single specific condition.",
  "submission": {
    "graphinvent_path": "./src/graphinvent/",
    "data_path": "./data/datasets/",
    "dataset": "chembl_v34_cond",
    "job_name": "cond_qed_high",
    "use_slurm": false,
    "slurm": {
      "account": "XXXXXXXXXX",
      "run_time": "0-01:00:00",
      "gpus_per_node": "T4:1"
    }
  },
  "job": {
    "job_type": "generate",
    "sample_mode": "generate",
    "device": "cuda",
    "batch_size": 1000,
    "n_samples": 1000,
    "n_workers": 0,
    "_checkpoint_note": "Update this path to the best epoch from the conditional training run. The condition_dim and condition_embedding_dim are loaded from the checkpoint.",
    "pretrained_model_path": "./output/chembl_v34_cond/conditional/run/model_restart_100.pth",
    "_conditions_note": "sample_conditions specifies the target property vector for generation. Property names must match the column names used during preprocessing (in order). Values should be normalised the same way as during training.",
    "sample_conditions": {
      "QED": 0.85,
      "SA_norm": 0.8,
      "LogP_norm": 0.5
    }
  }
}
